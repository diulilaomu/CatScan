# CatScan Windows客户端依赖
# Python 3.8+

# Web框架
eel>=0.16.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 键盘输入
pynput>=1.7.6

# Windows GUI（可选，用于窗口最大化）
pywin32>=306; sys_platform == 'win32'

# 其他依赖
python-dateutil>=2.8.2
